──────────────────────────────────────────
OUTPUT SECTION 42 — ULTRA SYSTEM ARCHITECT FLOWCHART
──────────────────────────────────────────
The following Mermaid diagram maps the entire flow of data from raw student logs through the Dual-Track Arbitrator, terminating in the LLM-generated pedagogical feedback. The ultimate goal of this pipeline is to mathematically link Moodle behavior with NLP writing quality to produce actionable, precise feedback.

```mermaid
graph TD
    %% 1. Data Sources
    subgraph 1_RAW_DATA [1. Raw Data Input]
        A1[Moodle Behavior Logs <br> time_on_task, clicks, sessions]
        A2[Student Essay Drafts <br> raw text per task, N=437]
    end

    %% 2. Feature Extraction
    subgraph 2_FEATURE_EXTRACTION [2. Feature Extraction Modules]
        B1[Adnan: NLP Pipeline <br> spaCy, TTR, Cohesion, Error Density]
        B2[Abdelkader: Activity Mining <br> PrefixSpan, Session Splits, PCA]
        A2 --> B1
        A1 --> B2
        B1 --> B3[Youcef: Temporal Trajectories <br> Slopes, Percentile Norms, 18 Rules]
        B2 --> B3
    end

    %% 3. Dual-Track Inference
    subgraph 3_DUAL_TRACK [3. The Dual-Track Inference]
        C1[Track B: Deterministic NLP/Rules <br> 18 Rules, Regex Rubrics, Composites]
        C2[Track A: Machine Learning <br> Random Forest, Clustering, Bayesian]
        B3 --> C1
        B3 --> C2
    end

    %% 4. The Arbiter
    subgraph 4_THE_ARBITER [4. The Preference System Arbiter]
        D1{Confidence Check & Comparator}
        C1 --> D1
        C2 --> D1
        D1 -->|High ML Confidence| D2[ML Output Chosen]
        D1 -->|Low ML/High Variance| D3[NLP Output Chosen]
        D2 --> E1[output_final.json]
        D3 --> E1
    end

    %% 5. LLM Feedback Generation
    subgraph 5_LLM_GENERATOR [5. LLM API Feedback Engine]
        E1 --> F1[LLM API Engine <br> Prompt: Link Behavior to Writing Errors]
        F1 --> G1[Individual Task Feedback <br> specific to draft]
        F1 --> G2[Individual Summative Feedback <br> final trajectory]
        F1 --> G3[Group/Cluster Feedback <br> shared cohort errors]
    end

    %% 6. Pedagogical Action
    subgraph 6_PEDAGOGICAL_ACTION [6. Pedagogical Action Loop]
        G1 --> H1[Teacher Dashboard Validation]
        G2 --> H1
        G3 --> H1
        H1 --> I1([Student Revision & SRL Growth])
        I1 -.->|Submits Next Draft| A1
        I1 -.->|Submits Next Draft| A2
    end

    classDef default fill:#1E1E1E,stroke:#4CAF50,stroke-width:2px,color:#FFF;
    classDef raw fill:#2196F3,stroke:#1565C0,color:#FFF;
    classDef llm fill:#9C27B0,stroke:#6A1B9A,color:#FFF;
    classDef arbiter fill:#F44336,stroke:#C62828,color:#FFF;
    
    class A1,A2 raw;
    class F1,G1,G2,G3 llm;
    class D1 arbiter;
```

